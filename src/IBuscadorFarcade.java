public interface IBuscadorFarcade {
    // Creo el método que despues las clases que implementan esta interface tienen que rellenar c/la logica correspondiente
    public void buscarDisponibilidad(String destino, String fechaDesde, String fechaHasta, String origen);
}
